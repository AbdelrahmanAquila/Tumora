import 'package:flutter/material.dart';

class CuraChat extends StatelessWidget {
  const CuraChat({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Cura Placeholder Screen'),
      ),
    );
  }
}
